package lokachop.betterthanwarfare.items.ammo;

import net.minecraft.core.item.Item;

public class LeatherPouchEmpty extends Item {
	public LeatherPouchEmpty(String translationKey, String namespaceId, int id) {
		super(translationKey, namespaceId, id);
		this.maxStackSize = 1;
	}
}
